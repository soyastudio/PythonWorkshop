# Python Workshop

## Install Required Libraries


```commandline
pip install numpy

pip install pandas

pip install scipy

pip install matplotlib

pip install seaborn

pip install mysql-connector-python

pip install pymongo

pip install ten

```


```commandline
curl https://api.deepseek.com/chat/completions -H "Content-Type: application/json" -H "Authorization: Bearer sk-7421b4b9ab364021bbcc5c362b1573cf" -d "{"model": "deepseek-chat","messages": [{"role": "system", "content": "You are a helpful assistant."},{"role": "user", "content": "Hello!"}],"stream": false}"

```
